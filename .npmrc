# Flat, npm-style node_modules so every workspace package can resolve shared
# dev tooling (typescript, vitest, tsx) and hoisted deps without re-declaring
# them everywhere. Published packages (@jabol/core, jabol) still declare their
# own runtime deps explicitly, which is what consumers install.
node-linker=hoisted
