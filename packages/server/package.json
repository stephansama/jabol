{
  "name": "@jabol/server",
  "version": "0.1.16",
  "description": "jabol Hono server — JSON API + per-request head-injected SPA shell.",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.0",
    "@jabol/core": "workspace:*",
    "better-auth": "^1.2.0",
    "better-sqlite3": "^12.0.0",
    "chokidar": "^4.0.1",
    "hono": "^4.6.0",
    "zod": "^4.4.3"
  }
}
